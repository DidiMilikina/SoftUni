function compundInt(arr){
    let principalSum = arr[0];
    let interestRatePercent = arr[1];
    let compoundingPeriod = arr[2];
    let totalTimespanInYears = arr[3];
    interestRatePercent /= 100;
    let frequency = 12 / compoundingPeriod;

   
}

//arr([1500, 4.3, 3, 6]);